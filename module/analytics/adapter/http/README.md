# Analytics HTTP Adapter

Exposes analytics status and seed endpoints.

## Key methods / endpoints / events
- Methods:
  - `NewHandler(...)`
  - `(*Handler).RegisterRoutes(...)`
- Endpoints:
  - `GET /analytics/status`
  - `POST /analytics/seed`
- Events: none.
