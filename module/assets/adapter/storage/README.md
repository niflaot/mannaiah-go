# assets/adapter/storage

Adapter for using core storage providers behind the assets module storage port.

## Key methods / endpoints / events
- Methods: `NewCoreStoreAdapter(store)`, `(*CoreStoreAdapter).Upload`, `(*CoreStoreAdapter).Delete`, `(*CoreStoreAdapter).Exists`, `(*CoreStoreAdapter).AvailabilityError`
- Endpoints: none.
- Events: none.
