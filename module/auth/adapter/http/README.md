# auth/adapter/http

Fiber HTTP adapter for authentication utility endpoints.

## Key methods / endpoints / events
- Methods: `NewHandler(service)`, `(*Handler).RegisterRoutes`
- Endpoints: `GET /check-auth`
- Events: none.
