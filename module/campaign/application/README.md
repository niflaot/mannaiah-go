# Campaign Application Package

Implements campaign CRUD and async send orchestration.

## Key methods / endpoints / events
- Methods:
  - `NewService(repository, resolver, sender, workers)`
  - `(*CampaignService).Create(...)`
  - `(*CampaignService).Send(...)`
- Endpoints: none.
- Events: none.
