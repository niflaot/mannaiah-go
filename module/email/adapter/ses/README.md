# Email SES Adapter

AWS SES provider adapter implementing the email provider port.

## Key methods / endpoints / events
- Methods:
  - `NewProvider(ctx, cfg)`
  - `(*Provider).Send(...)`
- Endpoints: none.
- Events: none.
