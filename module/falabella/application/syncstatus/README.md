# Sync Status Application Package

Application namespace for Falabella async feed status resolution use cases.

## Key methods / endpoints / events
- Methods: `service.NewService`, `service.RecordEntry`, `service.GetByFeedID`, `service.GetByProductID`, `service.ResolveFeedStatus`
- Endpoints: none (exposed through HTTP adapter)
- Events: none
