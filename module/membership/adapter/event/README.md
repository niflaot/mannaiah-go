# Membership Event Adapter

Publishes membership integration events to the core messaging bus.

## Key methods / endpoints / events
- Methods:
  - `NewPublisher(publisher)`
  - `(*Publisher).Publish(...)`
- Endpoints: none.
- Events:
  - `membership.v1.changed`
