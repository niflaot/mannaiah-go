# Membership Store Adapter

GORM repository for membership stamps and status snapshots.

## Key methods / endpoints / events
- Methods:
  - `NewRepository(db)`
  - `(*Repository).SaveStamp(...)`
  - `(*Repository).GetStatus(...)`
  - `(*Repository).ListStamps(...)`
- Endpoints: none.
- Events: none.
