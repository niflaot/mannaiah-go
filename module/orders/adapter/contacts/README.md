# Orders Contacts Adapter Package

`module/orders/adapter/contacts` adapts contacts services as order customer-source providers.

## Key Methods / Endpoints / Events
- Methods:
  - `contacts.NewSource(service)`
  - `(*contacts.Source).GetByID(ctx, id)`
- Endpoints: none in this package.
- Events: none in this package.
