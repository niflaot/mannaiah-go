# Orders Products Adapter Package

`module/orders/adapter/products` resolves order items against normalized product storage.

## Key Methods / Endpoints / Events
- Methods:
  - `products.NewResolver(db)`
  - `(*products.Resolver).Resolve(ctx, sku, alternateName)`
- Endpoints: none in this package.
- Events: none in this package.
