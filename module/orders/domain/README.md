# Orders Domain Package

`module/orders/domain` defines normalized order aggregate entities and invariants.

## Key Methods / Endpoints / Events
- Methods:
  - `(*domain.Order).Normalize()`
  - `(domain.Order).Validate()`
- Endpoints: none in this package.
- Events: none in this package.
