# products/application/product

Product use cases for create/read/list/update/delete orchestration.

## Key methods / endpoints / events
- Methods: `NewService(repository)`, `(*ProductService).Create`, `(*ProductService).Get`, `(*ProductService).List`, `(*ProductService).Update`, `(*ProductService).Delete`
- Endpoints: used by `/products` endpoints.
- Events: none.
