# products/application/variation

Variation use cases for create/read/list/update/delete orchestration.

## Key methods / endpoints / events
- Methods: `NewService(repository)`, `(*VariationService).Create`, `(*VariationService).Get`, `(*VariationService).List`, `(*VariationService).Update`, `(*VariationService).Delete`
- Endpoints: used by `/variations` endpoints.
- Events: none.
