# products/domain/product

Domain entity and invariants for products.

## Key methods / endpoints / events
- Methods: `(*Product).Normalize()`, `(Product).Validate()`, `MergeDatasheets(existing, incoming)`
- Endpoints: consumed by `/products` handlers.
- Events: none.
