# products/domain/variation

Domain entity and invariants for product variations.

## Key methods / endpoints / events
- Methods: `(*Variation).Normalize()`, `(Variation).Validate()`
- Endpoints: consumed by `/variations` handlers.
- Events: none.
