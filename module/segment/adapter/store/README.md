# Segment Store Adapter

GORM repository for segment definitions.

## Key methods / endpoints / events
- Methods:
  - `NewRepository(db)`
  - `(*Repository).Create(...)`
  - `(*Repository).GetByID(...)`
  - `(*Repository).List(...)`
  - `(*Repository).Update(...)`
  - `(*Repository).Delete(...)`
- Endpoints: none.
- Events: none.
