# Segment Application Package

Implements segment CRUD and resolution use-cases.

## Key methods / endpoints / events
- Methods:
  - `NewService(repository, resolver, rfmGroupRepository?)`
  - `(*SegmentService).Create(...)`
  - `(*SegmentService).Resolve(...)`
- Endpoints: none.
- Events: none.
