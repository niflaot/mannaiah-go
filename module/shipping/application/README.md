# Shipping Application Package

`application` defines shipping use-case namespaces.

## Key Methods / Endpoints / Events
- Methods:
  - `service.NewService(gateways)`
  - `(*service.RateQuoteService).Quote(ctx, request)`
- Endpoints: none in this package.
- Events: none in this package.
