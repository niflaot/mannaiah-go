# Sync Record HTTP Adapter

Exposes sync run query endpoints.

## Key methods / endpoints / events
- Methods:
  - `NewHandler(...)`
  - `(*Handler).RegisterRoutes(...)`
- Endpoints:
  - `GET /syncrecord/runs`
  - `GET /syncrecord/runs/:id`
  - `GET /syncrecord/stats`
- Events: none.
