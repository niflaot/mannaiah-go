# Sync Record Domain Package

Contains sync run aggregate primitives and enum value objects.

## Key methods / endpoints / events
- Methods:
  - `SyncKind.IsValid()`
  - `SyncKind.Module()`
  - `SyncTrigger.IsValid()`
  - `RunStatus.IsTerminal()`
- Endpoints: none.
- Events: none.
